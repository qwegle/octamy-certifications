# Retired scaffold setup

> **RETIRED — DO NOT USE.** `octamy-mobile/OctamyMobile` is superseded by the Expo project in `mobile/`. No feature, integration, offline, notification, payment, or release claim in the old scaffold should be treated as current.

Use `mobile/README.md` for installation and release limitations, and `mobile/docs/API-CONTRACT.md` for routes verified against the current Express server.
