# Public Octamy API origin only. Never put tokens, gateway secrets, or private keys in EXPO_PUBLIC_*.
# Local development may use HTTP; production builds require HTTPS.
EXPO_PUBLIC_API_URL=http://localhost:8080
